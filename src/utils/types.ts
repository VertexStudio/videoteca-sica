export default interface IVideo {
    id: number,
    title: string,
    src: string,
    description: string
    tags: string[],
    image: string,
}

export interface ICard {
    title: string,
    subtitle: string,
    url: string,
    
}